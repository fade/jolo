* emacsclient -nw dies instantly on GTK3 daemon build       :emacs:gtk3:terminal:
:PROPERTIES:
:DATE: 2026-03-26
:END:

After switching from emacs-pgtk to gtk3, =emacsclient -nw= opens a terminal
frame and immediately exits with no visible error.

** Root cause
=bergheim/disable-pgtk-im= is on =after-make-frame-functions=. It calls
=pgtk-use-im-context= which is defined in the GTK3 build (function exists)
but throws "Frames are not in use or not initialized" on terminal frames.
The =fboundp= guard passes, the call errors, and the error kills the new frame.

Defined in =~/.config/emacs/modules/completion.el:3-7=.

** Fix
Add =display-graphic-p= guard so it only runs on GUI frames:

#+begin_src elisp
(defun bergheim/disable-pgtk-im (&rest _args)
  (when (and (display-graphic-p) (fboundp 'pgtk-use-im-context))
    (pgtk-use-im-context nil)))
#+end_src

** Quick workaround (per session)
#+begin_src sh
emacsclient -e '(remove-hook (quote after-make-frame-functions) (quote bergheim/disable-pgtk-im))'
#+end_src

* Mixed-language pre-commit config dropped prose hooks     :precommit:prose:dx:
:PROPERTIES:
:DATE: 2026-02-15
:END:

Generating pre-commit config with mixed languages (example:
=["prose", "python"]=) omitted prose hooks, so =markdownlint= was missing.

** Root cause

=generate_precommit_config()= had a prose-only guard:

#+begin_src python
if lang == "prose" and languages != ["prose"]:
    continue
#+end_src

That condition skipped prose hooks whenever prose was combined with another
language.

** Fix

Removed the guard so prose hooks are included whenever =prose= is selected.

** Validation

- Targeted tests:
  - =tests/test_templates.py::TestGeneratePrecommitConfig::test_all_languages_combined=
  - =tests/test_templates.py::TestGeneratePrecommitConfig::test_prose_with_python=
- Full suite: =337 passed=

** Key files

- =_jolo/templates.py=
- =tests/test_templates.py=

* Rename notify-done to notify                                :notifications:naming:
:PROPERTIES:
:DATE: 2026-02-15
:END:

Renamed =container/notify-done= to =container/notify=. Installed at
=/usr/local/bin/notify=. No collision with =notify-send= (the system
desktop notification tool).

** Scope of rename
All references updated: Containerfile, tmux-layout.sh, setup.py hook
injection, commands.py spawn logic, tests. Historical docs (RESEARCH.org
archive entries, openspec archive) left as-is.

** Idempotency guards updated
The =setup_notification_hooks()= guards now check for ="notify"= substring
instead of ="notify-done"=. This means existing containers with old
=notify-done= hooks will get duplicate hooks on next =jolo up --sync=.
Acceptable since the old binary won't exist after image rebuild.

** Image rebuild required
The installed binary in the container image is stale — still the old version
without the =Click:= header (commit URL click-through). Rename + click URL
feature both require an image rebuild.

* devcontainer up hangs on Podman start event                :podman:devcontainer:
:PROPERTIES:
:DATE: 2026-02-09
:END:

Intermittent hangs (~50/50) during =jolo create= / =devcontainer up= after the "Container started" log line.

** Root cause
The =@devcontainers/cli= uses =podman events --filter event=start= to track readiness. In rootless Podman environments, the default =events_logger= is often =journald=, which can silently drop events or have delivery delays. If the container starts before the listener is attached or the event is dropped, the CLI hangs indefinitely.

** Fix/Workaround
1. Switch Podman to file-based logging in =~/.config/containers/containers.conf=:
   #+begin_src toml
   [engine]
   events_logger = "file"
   #+end_src
2. Implement a timeout + =podman inspect= readiness check in the =jolo= wrapper as a fail-safe.

** Refs
- [[https://github.com/microsoft/vscode-remote-release/issues/7980][microsoft/vscode-remote-release#7980]]
- [[https://github.com/devcontainers/cli/issues/816][devcontainers/cli #816]]
- [[https://github.com/containers/podman/issues/21685][containers/podman#21685]]

* Gemini CLI crashes on Alpine/musl                        :musl:nodejs:gemini:
:PROPERTIES:
:DATE: 2026-02-09
:END:

node-pty prebuilt binary segfaults during PTY cleanup on musl libc.
The command itself executes fine — crash happens on PTY destroy/resize after exit.

** Root cause

~@lydell/node-pty-linux-x64/pty.node~ is compiled against glibc.
~ldd~ shows missing symbols: ~__asprintf_chk~, ~fcntl64~ (glibc-specific),
plus ~napi_*~ symbols (resolved by Node at runtime, not an issue).

~gcompat~ makes the binary loadable but ~forkpty()~ still segfaults
at the native level during cleanup — uncatchable by JS try/catch.

** Fix

Set ~tools.shell.enableInteractiveShell: false~ in ~settings.json~.
This bypasses node-pty entirely and uses ~child_process.spawn()~ fallback
(see ~shellExecutionService.js:86-99~).

Applied in ~_jolo/setup.py:setup_credential_cache()~ — merges the setting
into gemini's settings.json when copying credentials to container.

** Key files

- ~_jolo/setup.py~ — merges ~enableInteractiveShell: false~ after credential copy
- ~Containerfile~ — ~procps~ (GNU pgrep), ~diffutils~ (GNU diff for apheleia)
- Gemini source: ~shellExecutionService.js~, ~getPty.js~, ~shell-utils.js~

** Refs

- https://github.com/google-gemini/gemini-cli/issues/14087 (Alpine crash, open)
- https://github.com/google-gemini/gemini-cli/issues/4676 (Alpine shell hang)
- https://github.com/google-gemini/gemini-cli/issues/3448 (bash not found)

* jolo --sync now refreshes template skills               :jolo:skills:sync:
:PROPERTIES:
:DATE: 2026-02-09
:END:

Added template skill syncing to all --sync flows so projects created earlier
can pick up new skills (e.g., research, feature-workflow) without re-creating.
Also handles dangling .claude/.gemini skills symlinks by checking for symlink
existence before creating new ones.

** Behavior
- `jolo up --sync`, `jolo tree --sync`, `jolo init --sync`, `jolo spawn --sync`
  call `sync_skill_templates()` to copy template skills into `.agents/skills`.
- Ensures `.claude/skills` and `.gemini/skills` symlinks exist.

** Key files
- `_jolo/setup.py` — `sync_skill_templates()` implementation
- `_jolo/commands.py` — hook in --sync paths

** What ~enableInteractiveShell: false~ loses

- Color output (stripped to plain text)
- Interactive programs (vim, less) don't work in shell tool
- ~isatty()~ returns false

None of this matters for AI agent usage in YOLO mode.

* BusyBox vs GNU tools in Alpine container                 :alpine:busybox:
:PROPERTIES:
:DATE: 2026-02-09
:END:

Alpine ships BusyBox which lacks many GNU flags. Tools that break:

| Tool | Missing flag | Fix package |
|------+--------------+-------------|
| pgrep | ~-g~ (process group) | ~procps~ |
| diff | ~--rcs~, ~--strip-trailing-cr~, ~--text~ | ~diffutils~ |

Emacs apheleia uses ~diff --rcs~ for formatting patches — fails silently
with BusyBox, shows "Output file descriptor of apheleia-diff is closed".

* DONE OAuth token expiration in devcontainers          :oauth:claude:credentials:
:PROPERTIES:
:DATE: 2026-02-09
:END:

Claude Code OAuth tokens expire multiple times per day in jolo devcontainers.

** Root cause

=setup_credential_cache()= copies =~/.claude/.credentials.json= to
=.devcontainer/.claude-cache/= at launch. The copy is bind-mounted RW into
the container. When Claude Code refreshes the token inside the container,
the new token writes to the copy — NOT back to host =~/.claude/=.
Next =jolo up= overwrites the refreshed token with stale host credentials.

Additionally, Claude Code has bugs where it doesn't actually use the refresh
token in several scenarios (copied creds, multi-instance, exit/restart).

** Fix (implemented)

Replaced the single =.claude-cache= directory mount with selective file mounts:
- =~/.claude/.credentials.json= — mounted RW from host (token refreshes persist)
- =~/.claude/statsig/= — mounted RO from host (feature flags)
- =settings.json= — still copied to =.claude-cache/= (notification hooks injected)
- =.claude.json= — still copied (MCP config injection)

This keeps =projects/=, =history.jsonl=, =todos/= invisible to the container
while letting token refreshes write back to the host.

** Key files
- =_jolo/constants.py= — =BASE_MOUNTS= (3 selective mounts replace 1 dir mount)
- =_jolo/setup.py= — =setup_credential_cache()= no longer copies credentials

** Open issues (anthropics/claude-code)

- [[https://github.com/anthropics/claude-code/issues/21765][#21765]] — refresh token not used on copied credentials (thumbs-upped, subscribed)
- [[https://github.com/anthropics/claude-code/issues/16957][#16957]] — exit-time refresh tokens not persisted
- [[https://github.com/anthropics/claude-code/issues/22600][#22600]] — multi-instance race condition
- [[https://github.com/anthropics/claude-code/issues/22602][#22602]] — VS Code reuses expired tokens across windows

* OpenSpec / Spec-Driven Development trial                 :sdd:openspec:workflow:
:PROPERTIES:
:DATE: 2026-02-09
:END:

Tried OpenSpec v1.1.1 as a Spec-Driven Development framework for this project.
Full cycle on the credential mount change: new → ff → apply → verify → archive.

** What it is

OpenSpec creates structured markdown artifacts (proposal → design → specs → tasks)
in =openspec/= before implementation. Specs accumulate in =openspec/specs/= as a
living system description. Supports Claude, Gemini, Codex via slash commands.

Install: =pnpm install -g @fission-ai/openspec@latest=
Init: =openspec init --tools claude,codex,gemini=

** What we learned

The credential mount change required ~10 lines of code changes. OpenSpec generated
17KB / 359 lines of spec artifacts across 7 files. 100:1 ceremony-to-code ratio.

The verify step caught one real issue (stale docstring). A =git diff= review
would have caught the same thing.

** Verdict: not for this project

The existing stack (AGENTS.md + TODO.org + feature-workflow skill) covers 80% of
the value with zero overhead. The TODO.org entry for this feature already had
design tradeoffs, upstream issue links, and alternatives — which is most of what
OpenSpec's proposal + design artifacts provided, just in a different format.

** Where it might earn its keep

- 3+ parallel agents on the same feature needing a shared checklist
  (=tasks.md= survives context resets, which is genuinely useful)
- Projects where design rationale needs to survive beyond chat history
  and is non-obvious from the code
- Large teams where multiple people need to stay aligned on what to build

** Files

OpenSpec files are in =openspec/= on the =sdd= branch as a reference.

* Generated project docs layout                             :templates:structure:
:PROPERTIES:
:DATE: 2026-02-09
:END:

Moved =TODO.org= and =RESEARCH.org= from project root to =docs/= for
generated projects. Keeps root clean for code files.

** Changes made

- =templates/TODO.org= -> =templates/docs/TODO.org=
- Created =templates/docs/RESEARCH.org=
- =templates/AGENTS.md= updated to reference =docs/= paths
- =_jolo/setup.py:copy_template_files()= now copies =docs/= as template dir
- Meta-project now also keeps TODO/RESEARCH under =docs/= for consistency

* jolo create slowness from Emacs cache copy             :emacs:perf:jolo:
:PROPERTIES:
:DATE: 2026-02-09
:END:

Observed ~jolo create~ taking ~40s while ~devcontainer up~ is ~2s and
~podman run~ is ~0.5s.

** Root cause
Emacs cache (~/.cache/emacs) was being copied as part of Emacs config staging.
Cache size was ~4.2G. Copy dominates runtime even if config itself is small.

** Fix
Exclude cache-like directories during Emacs config copy (e.g. elpaca/straight/elpa,
eln-cache, tree-sitter, auto-save-list, tramp, server). Keep caches in
~/.cache/emacs-container~ bind mounts instead of copying into workspace.

** Evidence
~rsync~ of ~~/.config/emacs~ alone is ~0.05s, while cache size is ~4.2G.

** Key files
- ~_jolo/setup.py~ (setup_emacs_config ignore patterns)

* Eliminate per-project Dockerfile and devcontainer exec overhead :performance:jolo:podman:
:PROPERTIES:
:DATE: 2026-02-09
:END:

Additional startup optimizations beyond the emacs cache fix.

** Per-project Dockerfile eliminated

Each project generated a Dockerfile (~FROM emacs-gui~ + ~apk add nodejs npm~ +
user label). The devcontainer CLI ran two builds (features layer + UID remap)
on every launch even when cached. Switched to ~"image"~ in devcontainer.json
with ~"updateRemoteUserUID": false~. ~DOCKERFILE_TEMPLATE~ constant removed.

** Direct podman exec instead of devcontainer CLI

Every ~devcontainer exec~ spawns Node.js, parses config, resolves features.
- ~is_container_running()~ now uses ~podman ps --filter~ directly
- ~_runtime_exec()~ helper tries ~podman exec~ first, falls back to devcontainer CLI
- Init commands batched into single exec with ~&&~

** GPG keyserver fetch removed from entrypoint

~gpg --keyserver keys.openpgp.org --recv-keys~ ran on every boot. Public keyring
already mounted read-only from host — fetch was redundant and caused variable
3s-2min delays depending on network.

** devcontainer CLI post-start delays (Arch Linux + podman)

Research found three CLI-side delays worth investigating if startup is still slow:
- ~updateRemoteUserUID~ CLI default may override JSON setting
  (use ~--update-remote-user-uid-default never~)
- ~userEnvProbe~ runs interactive login shell with 10s timeout
  (set ~"userEnvProbe": "none"~ in devcontainer.json)
- Podman ~events_logger=journald~ on Arch can stall 10-20s
  (set ~events_logger = "file"~ in ~containers.conf~)
- Ref: [[https://github.com/microsoft/vscode-remote-release/issues/7980][vscode-remote-release#7980]] (Arch + podman events hang)

** Rename

~run_default_mode~ renamed to ~run_up_mode~ — ~--help~ is the actual default,
~up~ is the subcommand.

* Agent session-end hooks (Claude / Gemini / Codex)      :agents:hooks:
:PROPERTIES:
:DATE: 2026-02-09
:END:

** Claude Code (confirmed)

Claude Code supports hooks configured in:
- =~/.claude/settings.json=
- =.claude/settings.json=
- =.claude/settings.local.json=

Hook events include =Stop=, =SessionStart=, and =SessionEnd=. The official
hooks reference documents the hook events, configuration structure, and that
=SessionEnd= runs when a session ends. citeturn1search0

** Gemini CLI (confirmed)

Gemini CLI documents lifecycle hooks including =SessionStart= and =SessionEnd=,
configured in =~/.gemini/settings.json= or =.gemini/settings.json=. The hooks
reference specifies event names and configuration structure. citeturn0search1turn0search8

** Codex CLI (no official hooks documented)

OpenAI’s official Codex CLI docs do not mention hooks or a notify configuration.
Treat any notification mechanism as unsupported until OpenAI documents it. citeturn0search6

* Zimfw race condition on container startup                :zsh:zimfw:perf:race:
:PROPERTIES:
:DATE: 2026-02-09
:END:

Multiple tmux windows starting simultaneously caused sporadic ~git symbolic-ref~
errors from zimfw.

** Symptoms

#+begin_example
x modules/input: Error during git symbolic-ref. Use zmodule option -z|--frozen...
  fatal: ref refs/remotes/origin/HEAD is not a symbolic ref
#+end_example

Different number of modules fail each time. Started appearing after Emacs config
copy was optimized (faster startup = more concurrent shell inits).

** Root cause

Tmuxinator launches 5 windows (emacs, claude, gemini, codex, shell) simultaneously.
Each window starts zsh, which sources ~.zshrc~. The ~.zshrc~ zimfw bootstrap
(lines 121-138) checks if ~init.zsh~ is older than ~.zimrc~ and runs
~zimfw init -q~ if so. With 5 shells racing, multiple ~zimfw init~ processes
do concurrent git operations on the same module repos under ~~/.zim/modules/~.

The ~pre~ action in ~zimfw.zsh~ (line 877) runs ~git symbolic-ref --short
refs/remotes/origin/HEAD~ on each module. Concurrent git operations on the
same repos corrupt or conflict on the ref files.

** Fix

Bake zimfw into the container image. Added to Containerfile:

#+begin_src dockerfile
COPY --chown=$USERNAME:$USERNAME container/zimrc $HOME/.zimrc
RUN curl -fsSL -o $HOME/.zim/zimfw.zsh --create-dirs \
        https://github.com/zimfw/zimfw/releases/latest/download/zimfw.zsh && \
    zsh -c "ZIM_HOME=$HOME/.zim source $HOME/.zim/zimfw.zsh init -q"
#+end_src

At runtime, ~init.zsh~ is already built and newer than ~.zimrc~, so the
~.zshrc~ check skips init entirely. No git operations, no race.

** What didn't work

- ~on_project_start~ in tmuxinator dev.yml — command was ignored or too late
- ~tmux-layout.sh~ pre-init with ~if [ -f zimfw.zsh ]~ — skipped on first boot
  because zimfw wasn't downloaded yet (it's not in dotfile mounts)

** Key files

- ~Containerfile~ — zimfw download + init at build time
- ~container/zimrc~ — module list baked into image
- ~container/tmux-layout.sh~ — reverted workaround
- ~~/.zshrc~ lines 121-138 — bootstrap code (bind-mounted from host, readonly)
- ~~/.zim/zimfw.zsh~ line 877 — the ~git symbolic-ref~ that races

* wt worktree manager permission denied on /workspaces    :wt:container:permissions:
:PROPERTIES:
:DATE: 2026-02-09
:END:

~wt new foo~ fails: ~fatal: could not create leading directories of '/workspaces/foo/.git': Permission denied~

** Root cause

~/workspaces/~ is owned by ~root:root~ with ~drwxr-xr-x~. The devcontainer
runtime creates it when mounting the project, defaulting to root ownership.
~wt~ creates worktrees as sibling dirs under ~/workspaces/~, which requires
write access to the parent.

** Fix

Added to Containerfile (root-level RUN block, before ~USER $USERNAME~):
#+begin_src dockerfile
mkdir -p /workspaces && chown $USERNAME:$USERNAME /workspaces
#+end_src

The devcontainer mount overlays the project subdirectory but the parent dir
retains its image-layer permissions.

Also replaced ~just tree~ recipe (which only called ~wt new~) with a full
passthrough ~just wt~ recipe that supports all subcommands.

** Key files
- ~Containerfile~ — ownership fix
- ~container/wt~ — worktree manager script
- ~justfile~ — ~wt~ recipe

* ntfy.sh agent completion notifications                   :ntfy:notifications:hooks:
:PROPERTIES:
:DATE: 2026-02-09
:END:

Push notifications when AI agents finish, via ntfy.sh HTTP API.

** Architecture
- ~container/notify-done~ — POSIX shell script, POSTs to ntfy.sh
- Hooks injected into agent settings at container launch time
- Claude: ~SessionEnd~ hook in ~.claude-cache/settings.json~
- Gemini: ~SessionEnd~ hook in ~.gemini-cache/settings.json~
- Codex: ~notify~ key in ~.codex-cache/config.toml~ (undocumented, best-effort)
- Env vars: ~NTFY_SERVER~ (from host), ~NTFY_TOPIC~ (defaults to project name), ~PROJECT~

** Key decisions
- ~SessionEnd~ not ~Stop~ for Claude — Stop fires on every response, spamming
  notifications in interactive sessions. Caught by Codex review.
- ~set -e~ + ~|| true~ on curl — script must never fail the agent hook
- ~--max-time 5~ on curl — prevents network hangs stalling agent shutdown
- ~[ ! -t 0 ]~ guard on stdin — prevents blocking when run manually
- ~_load_json_safe()~ — corrupt settings.json won't crash jolo up
- TOML check uses ~line.strip().startswith("notify")~ not substring match

** Self-hosted setup
User runs ntfy via docker compose on ~burial.ts.glvortex.net:9080~.
Host env: ~export NTFY_SERVER=http://burial.ts.glvortex.net:9080~
Phone app: ntfy Android via Obtainium from GitHub releases.

** Key files
- ~container/notify-done~ — notification script
- ~_jolo/setup.py~ — ~setup_notification_hooks()~ + ~_load_json_safe()~
- ~_jolo/container.py~ — NTFY env vars in containerEnv
- ~_jolo/commands.py~ — wired into all 5 launch flows
- ~docs/plan-notifications-claude.org~ — full design doc

* ntfy Click header works on self-hosted Android client     :ntfy:android:click:
:PROPERTIES:
:DATE: 2026-02-15
:END:

Tested ntfy Android (F-Droid) with self-hosted server.
Using the ~Click~ header opens the URL on tap. The same test against ~ntfy.sh~
did not show notifications in the client.

** What worked
- Send to self-hosted server (http://burial.ts.glvortex.net:9080), topic ~jolo~
- Include ~Click: https://github.com/<org>/<repo>/commit/<sha>~

** Notes
- Direct curl posts to ~https://ntfy.sh/jolo~ returned HTTP 200 but did not
  surface in the Android client.

** Branch Comparison Findings (notify-done vs feat/ntfy-notify-hooks) :musl:json:
The ~notify-done~ branch was chosen as the production path over ~feat/ntfy-notify-hooks~
due to several critical technical differences:

- **Hook Structure**: Claude and Gemini require a nested JSON structure for hooks:
  ~hooks > EventName > [ { hooks: [ { type: "command", command: "..." } ] } ]~.
  The ~feat~ branch used a flat array which is incompatible with current agent versions.
- **Script Robustness**: The ~notify-done~ script in the current branch correctly drains
  ~stdin~. Agents often pipe a JSON context (containing session IDs, etc.) to hooks.
  If the script doesn't read this, it can cause SIGPIPE or block the agent's exit.
- **Codex Support**: Both branches implemented Codex support by appending to
  ~.codex-cache/config.toml~, but the ~notify-done~ implementation uses the same
  environment variable pattern as the other agents for consistency.
- **File Permissions**: The ~feat~ branch accidentally changed the notification
  script to mode 644 (non-executable). Corrected to 755 in this branch.

* Devcontainer "Container started" sleep loop is not root hang :devcontainer:podman:debug:
:PROPERTIES:
:DATE: 2026-02-09
:END:

When ~devcontainer up~ starts a container, it injects a keep-alive command:

#+begin_src
/bin/sh -c 'echo Container started; trap "exit 0" 15; exec "$@"; while sleep 1 & wait $!; do :; done -'
#+end_src

This shows up in ~podman top~ as a ~sleep 1~ loop, but it is *normal* and
indicates the devcontainer CLI is keeping the container alive for exec/hooks.
It is not itself the cause of a hang.

** Evidence
- ~devcontainer up --log-level trace~ completed with ~"outcome":"success"~
  and ~LifecycleCommandExecutionMap~ empty (no hooks running).
- ~podman inspect <container> --format '{{.Config.Cmd}}'~ shows the keep-alive loop.

** Where the real hang likely is
If ~jolo create~ appears to hang after "Container started", the stall is
likely in the *next step* (e.g. a ~devcontainer exec~ that starts tmux,
tmuxinator, or shell init). Run the suspected exec with ~--log-level trace~
to surface the failing command.

** Useful commands
- ~devcontainer exec --log-level trace --workspace-folder <host-path> -- /bin/sh -lc '...command...'~
- ~podman exec -it <ctr> sh -lc 'ps -ef'~
- ~podman logs <ctr>~

* Eglot Python server prompt due to multiple servers        :emacs:eglot:python:
:PROPERTIES:
:DATE: 2026-02-09
:END:

Eglot prompts for Python server selection when multiple servers are available
(e.g. ~pyright-langserver~ and ~ruff~). This prevents auto-start on file open.

** Config locations
- Eglot config: =~/.config/emacs/modules/bergheim-eglot.el~
- Apheleia config (global mode): =~/.config/emacs/modules/formating.el~

** Current state
- Apheleia is enabled globally but only maps JS/TS/CSS/JSON to ~biome~.
- No Python formatter is configured in Apheleia.

** Fix
Pin Eglot to a single Python LSP (e.g. ~pyright-langserver~) to avoid prompts.
If using Apheleia for Python formatting (e.g. ~ruff format~), add a Python
formatter mapping in ~formating.el~ to avoid conflicts with LSP formatting.

* gh auth token not available in devcontainers             :gh:credentials:jolo:
:PROPERTIES:
:DATE: 2026-02-09
:END:

~gh auth token~ fails inside jolo devcontainers even though ~/.config/gh~ is
mounted read-only from host.

** Root cause

Newer ~gh~ versions store OAuth tokens in the OS keyring (e.g. ~gnome-keyring~,
~kwallet~, macOS Keychain) instead of writing them to ~hosts.yml~. The mounted
~hosts.yml~ contains user/protocol info but no ~oauth_token~ field. The container
has no access to the host keyring.

** Fix

Pass ~GH_TOKEN~ as a ~containerEnv~ variable, populated by ~get_secrets()~ which
tries (in order):
1. ~GH_TOKEN~ env var
2. ~GITHUB_TOKEN~ env var
3. ~gh auth token~ CLI output (reads from host keyring)

The ~${localEnv:GH_TOKEN}~ in devcontainer.json picks up the value set by
~os.environ.update(secrets)~ in the jolo launch flow.

** Key files
- ~_jolo/container.py~ — added ~GH_TOKEN~ to ~containerEnv~
- ~_jolo/setup.py~ — ~get_secrets()~ fetches token via ~gh auth token~
- ~.devcontainer/devcontainer.json~ — added ~GH_TOKEN~ entry

** Note
Existing containers need ~jolo up --sync --new~ to pick up the change.
The ~gh~ config mount (~~/.config/gh~ readonly) is kept for non-token config.

* Feature workflow reviews alternate agents               :process:skills:
:PROPERTIES:
:DATE: 2026-02-09
:END:

Updated the feature-workflow skill to require Review 2 to use a different
external agent than Review 1 to ensure fresh eyes on correctness.

** Key files
- .agents/skills/feature-workflow/SKILL.md

* Skills deduplication via symlink chain                :skills:architecture:
:PROPERTIES:
:DATE: 2026-02-11
:END:

Eliminated duplication between =.agents/skills/= and =templates/.agents/skills/=.
Skills are now authored once in =templates/.agents/skills/= and the rest is symlinks.

** Symlink chain
#+begin_example
templates/.agents/skills/<name>/SKILL.md   # single source of truth
.agents/skills -> ../templates/.agents/skills  # project root symlink
.claude/skills -> ../.agents/skills            # Claude discovery
.gemini/skills -> ../.agents/skills            # Gemini discovery
#+end_example

** Decision: AGENTS.md stays separate
=AGENTS.md= (meta-project) and =templates/AGENTS.md= (generated projects) have
genuinely different content. No symlink — each maintained independently.

* Ruff vs Apheleia formatting fight                     :ruff:emacs:pre-commit:
:PROPERTIES:
:DATE: 2026-02-11
:END:

Emacs save-on-format and pre-commit hooks disagreed on Python formatting,
causing every commit to fail with ruff-format changes.

** Root causes (three layered problems)

1. *Pre-commit used different ruff version*: pinned remote repo had ruff 0.8.6,
   system had 0.14.9. Fixed by switching to =repo: local= with =language: system=.

2. *Apheleia defaulted to black, not ruff*: =apheleia-mode-alist= maps
   =python-mode= to =black= by default. Black was installed in Alpine.
   Fixed by adding =(setf (alist-get 'python-mode apheleia-mode-alist) 'ruff)=
   to =~/.config/emacs/modules/formating.el=.

3. *Unstaged pyproject.toml caused stash revert*: =pyproject.toml= was changed
   from =line-length = 120= to =line-length = 79= but not staged. Pre-commit
   stashes unstaged changes (=git stash --keep-index=) before running hooks,
   reverting pyproject.toml to the committed 120 value. Emacs ruff read 79
   from the working tree; pre-commit ruff read 120 from HEAD.

** Fix

- =.pre-commit-config.yaml=: =repo: local=, =language: system= for ruff
- =formating.el=: explicit ruff mapping for python-mode and python-ts-mode
- Staged pyproject.toml + all reformatted files together in one commit

** Key insight

=apheleia-formatters-respect-fill-column= defaults to nil. Apheleia does NOT
pass =--line-length= to ruff. Both Emacs and pre-commit read line-length
from =pyproject.toml= — the only question is /which version/ of pyproject.toml
they see (working tree vs stashed HEAD).

** Key files
- =.pre-commit-config.yaml= — local system ruff hooks
- =pyproject.toml= — =line-length = 79=
- =~/.config/emacs/modules/formating.el= — ruff overrides for Python

* sync_skill_templates self-destructs via symlink          :skills:jolo:symlink:
:PROPERTIES:
:DATE: 2026-02-11
:END:

~sync_skill_templates~ in ~_jolo/setup.py~ crashed with ~FileNotFoundError~
when run on the meta-project (emacs-container itself).

** Root cause

In this repo, ~.agents/skills~ is a symlink to ~../templates/.agents/skills~.
The sync function's destination (~target_dir/.agents/skills~) resolves through
the symlink to the same path as the source (~templates/.agents/skills~). The
loop calls ~shutil.rmtree(dst)~ before ~shutil.copytree(entry, dst)~ — rmtree
destroys the source through the symlink, then copytree fails on the deleted dir.

** Fix

Resolve both paths and skip sync when they're identical (commit ~8317bc6~).

** Related

Commit ~1ee5d3a~ ("Remove local skills") also dropped ~afk~ and ~db-reset~
skills. They existed only in ~.agents/skills/~ (local) and were never copied
to ~templates/.agents/skills/~ before the local copies were deleted. Restored
in the same fix commit.

* TERM/term-keys mismatch in devcontainer                 :tmux:terminfo:emacs:
:PROPERTIES:
:DATE: 2026-02-11
:END:

User observed that Emacs + term-keys works over SSH but not inside devcontainer.
Recent local changes forced TERM=tmux-direct in the container and devcontainer env.
If tmux-direct terminfo is missing in the container, term-keys and Emacs key
translations can break.

** Current local diff
- Containerfile: added tmux-direct to terminal-features and set TERM=tmux-direct
- _jolo/container.py: containerEnv TERM set to tmux-direct

** Next checks
- Ensure /usr/share/terminfo/t/tmux-direct exists in container image
- If missing, either install terminfo or fallback to tmux-256color
- Verify tmux default-terminal and terminal-overrides for key passthrough

* Pre-commit hook caching requires PRE_COMMIT_HOME in container  :precommit:hooks:container:
:PROPERTIES:
:DATE: 2026-02-11
:END:

Warm-cache in the image alone did not prevent first-commit hook downloads.
Root cause: pre-commit uses =~/.cache/pre-commit= by default unless
~PRE_COMMIT_HOME~ is set inside the running container.

** Fix
- Bake hook environments into the image.
- Set ~PRE_COMMIT_HOME=/opt/pre-commit-cache~ in the devcontainer env
  so runtime uses the baked cache.

** Key files
- ~Containerfile~ (pre-commit install-hooks + cache path)
- ~_jolo/container.py~ (devcontainer.json env)

* DONE Claude CLI uses API key over Max subscription           :claude:auth:
:PROPERTIES:
:DATE: 2026-02-11
:END:

~claude -p~ returned "Credit balance is too low" because ~ANTHROPIC_API_KEY~
in the environment takes precedence over Max subscription OAuth.

** Credential precedence (hardcoded, no override)

1. ~ANTHROPIC_API_KEY~ (env var) — highest, always wins
2. ~CLAUDE_CODE_OAUTH_TOKEN~ (env var)
3. ~~/.claude/.credentials.json~ (stored OAuth from ~/login~)

No ~--use-subscription~ flag exists. ~forceLoginMethod: "claudeai"~ in
settings.json only affects the login screen, not runtime auth precedence.
Multiple GitHub issues requesting this (#12861, #9880, #8327) — all closed
with no resolution as of Feb 2026.

** Fix

Strip ~ANTHROPIC_API_KEY~ from the claude command via shell alias in
~Containerfile~. The key stays in the container env for Emacs and other tools.

#+begin_src dockerfile
alias claude="env -u ANTHROPIC_API_KEY claude --dangerously-skip-permissions"
#+end_src

** Refs
- [[https://github.com/anthropics/claude-code/issues/12861][#12861]] — Option to prefer subscription auth (closed/stale)
- [[https://github.com/anthropics/claude-code/issues/9880][#9880]] — Add flag to ignore API key (closed as duplicate)
- [[https://github.com/anthropics/claude-code/issues/8327][#8327]] — API key overrides Max/Pro subscription
- [[https://github.com/anthropics/claude-code/issues/16489][#16489]] — forceLoginMethod bug (open)

* Pre-commit hook warmup needs git repo in Containerfile      :precommit:container:hooks:
:PROPERTIES:
:DATE: 2026-02-11
:END:

~pre-commit install-hooks~ fails outside a git repo. Warmup in the image must
create a temporary git repo before installing hooks.

** Fix
- Run hook warmup in a temp git repo (pre-commit requires one):

#+begin_src dockerfile
RUN cd /tmp && git init pre-commit-repo && cd pre-commit-repo && \
    pre-commit install-hooks -c /tmp/pre-commit-hooks.yaml && \
    cd / && rm -rf /tmp/pre-commit-repo /tmp/pre-commit-hooks.yaml
#+end_src

- Remove biome/markdownlint hooks from warmup list — pre-commit's
  ~language: node~ hardcodes ~npm~ internally, and Alpine has pnpm not npm.
  ~language_version: system~ avoids the node download but still calls npm.
- Install ~markdownlint-cli~ via pnpm global instead.
- Projects use ~repo: local~ with ~language: system~ for node-based hooks.

** Key files
- ~Containerfile~ (hook warmup, pnpm globals, pre-commit-hooks.yaml)

* Stash shared mount for devcontainers                 :jolo:devcontainer:mounts:
:PROPERTIES:
:DATE: 2026-02-11
:END:

Added a default RW shared mount intended as a non-reproducible "stash"
for ad-hoc notes and scratch files.

** Decision
- Host path: ~/stash
- Container path: /workspaces/stash
- Default-on, explicitly labeled as non-reproducible in MOTD

** Implementation
- Added mount to ~BASE_MOUNTS~ (devcontainer.json generation)
- Ensure host directory exists before container start
- MOTD banner includes stash warning

** Key files
- ~_jolo/constants.py~ (BASE_MOUNTS)
- ~_jolo/setup.py~ (setup_stash)
- ~_jolo/commands.py~ (call setup_stash)
- ~container/motd~ (stash warning)

* Stash default path + init flow guard               :jolo:devcontainer:init:
:PROPERTIES:
:DATE: 2026-02-11
:END:

Renamed the shared mount from "junk drawer" to "stash" and ensured the
host path exists before devcontainer startup, including ~jolo init~.

** Decision
- Host path: ~/stash
- Container path: /workspaces/stash
- Always create host path via ~setup_stash()~ before ~devcontainer_up~

** Key files
- ~_jolo/constants.py~ (BASE_MOUNTS)
- ~_jolo/setup.py~ (setup_stash)
- ~_jolo/commands.py~ (call setup_stash in init/up/tree/create/spawn)
- ~container/motd~ (stash label)
- ~templates/AGENTS.md~ (stash guidance)

* wt new: single shell window instead of 5-window layout   :wt:tmux:fix:
:PROPERTIES:
:DATE: 2026-02-11
:END:

~wt new foo~ was cloning the full tmuxinator ~dev.yml~ config (emacs, claude,
gemini, codex, shell) into every worktree session. This floods the tmux
window list when creating multiple worktrees.

** Fix
Replaced tmuxinator-based ~create_session()~ with plain ~tmux new-session~.
Worktree sessions now get a single shell window. If ~-p~ is passed, the
window runs ~claude <prompt>~ instead of a shell. The main ~dev~ session
(created by ~tmux-layout.sh~) keeps the full layout.

** Key files
- ~container/wt~ (~create_session~ function)
* Command naming conventions for dev containers & CLIs          :cli:ux:conventions:
:PROPERTIES:
:DATE: 2026-02-15
:END:

** Summary
- Docker and Podman both use =attach= to connect to a running container’s stdio and =exec= to run a new command in a running container, so =attach= is the conventional verb for “connect to existing.”
- Docker Compose standardizes lifecycle as =up= and =down=, which map cleanly to “start/create” and “stop/remove.”
- Devcontainer CLI’s core commands are =up=, =build=, and =exec=; “open” is not a core CLI command, but a VS Code-specific =open-in-code= (and sometimes VS Code exposes an =open= alias) to open a devcontainer in the editor.

** Findings
- Docker CLI documents =docker attach= as attaching local stdin/stdout/stderr to a running container, and =docker exec= as executing a new command in a running container. [[https://docs.docker.com/reference/cli/docker/container/attach/][Docker CLI: attach]] [[https://docs.docker.com/engine/reference/commandline/exec][Docker CLI: exec]]
- Podman’s official commands list includes =attach= and =exec= for running containers, mirroring Docker’s verbs. [[https://docs.podman.io/en/stable/Commands.html][Podman commands]]
- Docker Compose standardizes lifecycle with =up= and =down=, where =down= stops and removes containers/networks created by =up=. [[https://docs.docker.com/reference/cli/docker/compose/down/][Docker Compose: down]]
- Devcontainer CLI help lists =up=, =build=, and =exec= as core commands; the VS Code docs note that =open= is only shown when installed via VS Code, and =open-in-code= is the CLI entry point to open a devcontainer in the editor. [[https://code.visualstudio.com/docs/devcontainers/devcontainer-cli][VS Code devcontainer CLI]] [[https://stuartleeks.github.io/devcontainer-cli/open-in-code.html][devcontainer open-in-code]]

** Links
- [[https://docs.docker.com/reference/cli/docker/container/attach/][Docker CLI: attach]]
- [[https://docs.docker.com/engine/reference/commandline/exec][Docker CLI: exec]]
- [[https://docs.docker.com/reference/cli/docker/compose/down/][Docker Compose: down]]
- [[https://docs.podman.io/en/stable/Commands.html][Podman commands]]
- [[https://code.visualstudio.com/docs/devcontainers/devcontainer-cli][VS Code devcontainer CLI]]
- [[https://stuartleeks.github.io/devcontainer-cli/open-in-code.html][devcontainer open-in-code]]

** Open Questions
- Should =jolo= add a dedicated =exec= verb (if not present) to align with container CLIs, or keep =up --run= as the primary mechanism?

* ntfy Actions header needed for web UI click-through      :ntfy:notifications:
:PROPERTIES:
:DATE: 2026-02-15
:END:

The ~Click:~ header only works on native mobile apps (Android/iOS).
On the ntfy web UI, clicking a notification copies its body text.

** Fix
Add ~Actions: view, Open commit, <URL>~ header alongside ~Click:~.
This renders a clickable button in ALL ntfy clients (web, Android, iOS).

#+begin_src sh
ACTIONS_HEADER="Actions: view, Open commit, ${COMMIT_URL}"
curl ... -H "$ACTIONS_HEADER" ...
#+end_src

Applied to ~container/notify~. Needs image rebuild to take effect in new containers.

** Stale image causes old-format notifications
The installed ~/usr/local/bin/notify~ was manually patched in the running
container, but the image itself was never rebuilt after the session-ID fix
(=75cdd74=) or Click header addition (=828a7ff=). New containers from the
stale image still send =Session: UUID= in the body and lack click URLs.

Verified with ntfy JSON poll: old-format messages from previous Claude
session used em-dash title and =Agent ... Session: UUID= body.

** Verification
Test notification to =/test= topic confirmed working:
- New format title (hyphen, not em-dash)
- Git commit message as body (no Session UUID)
- ~click~ field present in ntfy JSON
- ~Actions~ header pending (source updated, not yet installed)

* Ruff version drift between container and CI              :ruff:ci:precommit:
:PROPERTIES:
:DATE: 2026-02-15
:END:

CI format check failed because container had ruff 0.14.9 (Alpine package)
while CI installed latest (0.15.1 via ~pip install ruff~). Ruff 0.15 changed
lambda parenthesization rules.

** Root cause
- Alpine ~ruff~ package lagged behind PyPI (0.14.9 vs 0.15.1)
- Pre-commit used ~language: system~ (Alpine's stale ruff)
- CI used ~pip install ruff~ (latest from PyPI)
- Pre-commit cache in Containerfile had ancient pinned revs (ruff v0.8.6, etc.)

** Fix
- Removed ~ruff~ from ~apk add~, install via ~uv tool install ruff~ instead
- Updated all pre-commit cache priming revs to latest
- Added ~pre-commit autoupdate~ before ~install-hooks~ in Containerfile so
  builds always resolve latest tags regardless of hardcoded seeds
- Reformatted ~tests/test_container.py~ for ruff 0.15 lambda style

** Key files
- ~Containerfile~ — ruff install method, cache priming revs, autoupdate
- ~.github/workflows/test.yml~ — CI uses unpinned ~pip install ruff~
- ~tests/test_container.py~ — reformatted lambda parenthesization

* CI test failure: get_secrets mock leak in integration tests  :ci:testing:mock:
:PROPERTIES:
:DATE: 2026-02-15
:END:

~test_init_installs_test_hooks~ failed in CI but passed locally.

** Root cause
The test mocked ~subprocess.run~ globally, which intercepted ~get_secrets()~'s
~pass~ calls. The mock returned Mock objects for ~stdout.strip()~ instead of
strings. ~os.environ.update(secrets)~ then failed with ~TypeError: str expected,
not Mock~. Locally, ~pass~ is installed and returns real strings.

** Fix
Added ~get_secrets=mock.Mock(return_value={})~ to both ~_mock_devcontainer_calls~
(shared helper for create tests) and the init test's ~mock.patch.multiple~ block.

** Key files
- ~tests/test_integration.py~

* tmux terminal-features wildcard and host config override  :tmux:terminal:config:
:PROPERTIES:
:DATE: 2026-02-17
:END:

tmux =terminal-features= uses =fnmatch(3)= for pattern matching, so =*= works
as a wildcard for all terminal types. Replaced per-terminal listings with
=set -as terminal-features ",*:sixel:extkeys"= in =/etc/tmux.conf=.

** extkeys

Enables CSI u / xterm modifyOtherKeys. Lets tmux distinguish key combos that
are normally ambiguous (=C-S-a= vs =C-a=, =C-;=, =C-RET=). Required for
Emacs keybindings like =C-S-<return>= through tmux. Needs =set -s extended-keys on=
on the server side as well.

** Host config clobbers container /etc/tmux.conf

The host =~/.tmux.conf= is bind-mounted into jolo containers (readonly) and
the emacs-container yadm sandbox. tmux loads =/etc/tmux.conf= first, then
=~/.tmux.conf=. Any =set -g terminal-features= (without =-a=) in the user
config *replaces the entire array*, wiping =/etc/tmux.conf= settings.

Found in the yadm sandbox copy:
#+begin_src tmux
set -g terminal-features ',xterm-256color:sixel'
#+end_src
This nuked all prior settings (clipboard, extkeys from /etc/tmux.conf).

** Fix

Container =/etc/tmux.conf= (Containerfile):
#+begin_src tmux
set -g allow-passthrough on
set -as terminal-features ",*:sixel:extkeys"
#+end_src

Host =~/.tmux.conf= should:
1. Uncomment/broaden: =set -as terminal-features '*:extkeys:sixel:clipboard'=
2. Remove the clobbering =set -g terminal-features ',xterm-256color:sixel'=

** Host config is read-only in emacs-container

=/home/tsb/.tmux.conf= is EROFS in the emacs-container. Changes must be made
on the host directly.

* BETH scaffold plan review (openspec)                    :typescript:scaffold:elysia:
:PROPERTIES:
:DATE: 2026-02-16
:END:

Reviewed =openspec/changes/beth-stack-scaffold/plan.md= for implementation
risk before coding.

** What was investigated
- Whether the plan is implementation-ready against current scaffold architecture
  in =_jolo/templates.py= and =_jolo/commands.py=
- Whether TypeScript/Bun runtime and test flow are safe by default

** Key findings
- Tests need =src/index.tsx= to export =app= and gate =app.listen()= behind
  =if (import.meta.main)=. Otherwise importing in tests can bind ports and fail.
- Plan sets =jsxImportSource= to =@kitajs/html= but only installs
  =@elysiajs/html=. =@kitajs/html= should be an explicit dependency.
- Proposed =extra_files= in =get_test_framework_config('typescript')= works, but
  it mixes app scaffolding with test config responsibilities.

** Suggested fix direction
- Treat the first two findings as blockers before implementation.
- Keep scaffold file injection separate from test framework config where possible.
- Add explicit integration checks for generated TSX entrypoint behavior.

** Key files
- =openspec/changes/beth-stack-scaffold/plan.md=
- =_jolo/templates.py=
- =_jolo/commands.py=

* Host tmux.conf terminal-features clobber (post-rebuild audit) :tmux:terminal:clipboard:
:PROPERTIES:
:DATE: 2026-02-17
:END:

Post-rebuild audit of terminal setup. Container-side fixes from this branch
are working: =TERM=tmux-direct= comes from tmux's =default-terminal= (not
forced via env/shell), =/etc/tmux.conf= appends =*:sixel:extkeys=.

** Problem: host tmux.conf clobbers /etc/tmux.conf

The host =~/.tmux.conf= (bind-mounted into container) has a non-appending
=set -g terminal-features= near the end that wipes everything set earlier:

#+begin_src tmux
# This line (near bottom of host ~/.tmux.conf) resets the array:
set -g terminal-features ',xterm-256color:sixel'

# These earlier lines get wiped by the above:
set -as terminal-features ',*:clipboard'   # gone
# /etc/tmux.conf: set -as terminal-features ",*:sixel:extkeys"  # gone
#+end_src

Also redundant: the =Ms= terminal-override for OSC 52 is unnecessary when
=clipboard= is in terminal-features (tmux 3.2+).

** Outer client TERM

=tmux display -p '#{client_termname}'= shows =xterm= — this is what the
devcontainer runtime provides. The =terminal-overrides *:RGB= and =*:Ms=...=
wildcards compensate. The =terminal-features= wildcard =*:sixel:extkeys=
would also cover it, if not clobbered.

** Fix (on host, not container — EROFS here)

Remove from host =~/.tmux.conf=:
- =set -g terminal-features ',xterm-256color:sixel'= (clobbering line)
- =set -as terminal-features ',*:clipboard'= (consolidated)
- =set -ga terminal-overrides ',*:Ms=\E]52;%p1%s;%p2%s\007'= (redundant with clipboard feature)
- Commented-out =# set -as terminal-features 'xterm*:extkeys'=

Replace with single consolidated line:
#+begin_src tmux
set -as terminal-features ',*:clipboard:sixel:extkeys'
#+end_src

* Mosh OSC 52 clipboard with tmux                      :mosh:tmux:clipboard:
:PROPERTIES:
:DATE: 2026-02-17
:END:

Mosh 1.4.0 added OSC 52 clipboard support but only accepts the =c;= selection
parameter (e.g., =\e]52;c;BASE64\a=). tmux emits =\e]52;;BASE64\a= (empty
selection) which mosh silently drops.

** Root cause

tmux's =clipboard= terminal-feature (3.2+) uses a hardcoded built-in OSC 52
format with empty selection parameter AND ignores the =Ms= terminal-override.
So even with a correct =Ms= override, the =clipboard= feature bypasses it.

** Fix (host tmux.conf)

1. Remove =clipboard= from =terminal-features=:
   #+begin_src tmux
   set -as terminal-features ',*:sixel:extkeys'
   #+end_src

2. Add =Ms= override that hardcodes =c;= for mosh compatibility:
   #+begin_src tmux
   set -as terminal-overrides ',*:Ms=\\E]52;c%p1%.0s;%p2%s\\7'
   #+end_src

3. Keep =set-clipboard on=.

4. Kill tmux server (=tmux kill-server=) — overrides are read at server start.

The =c;= format works everywhere (mosh, SSH, direct terminal). All terminals
that support OSC 52 accept the =c= (clipboard) selection parameter.

** Mosh limitations

- Clipboard truncated at ~1.4 KB (UDP packet size limit)
- Copy-out only (cannot paste from local clipboard into mosh)
- PRs [[https://github.com/mobile-shell/mosh/pull/1054][#1054]] and [[https://github.com/mobile-shell/mosh/pull/1104][#1104]] fix the empty-selection issue but remain unmerged

** Key insight

=%.0s= in the =Ms= format consumes the selection parameter without printing it,
allowing =c= to be hardcoded. This is a terminfo formatting trick.

** Debugging OSC 52 through mosh

Bypass tmux's Ms/clipboard mechanism entirely:
#+begin_src sh
printf '\033]52;c;%s\007' $(echo -n "test" | base64)
#+end_src
If this copies to clipboard, mosh forwards OSC 52 correctly and the problem
is tmux's output format.

** Container /etc/tmux.conf

Keeps =clipboard= in terminal-features because the host =~/.tmux.conf=
(bind-mounted) handles the Ms override. Do not change container config.

** Refs
- [[https://github.com/mobile-shell/mosh/issues/637][Mosh #637]] — original OSC 52 request (2015)
- [[https://github.com/mobile-shell/mosh/pull/1054][Mosh PR #1054]] — tmux compatibility fix (unmerged)
- [[https://github.com/mobile-shell/mosh/pull/1104][Mosh PR #1104]] — improved selection types (unmerged)
- [[https://github.com/tmux/tmux/wiki/Clipboard][tmux clipboard wiki]]

* term-keys regression fixed in container defaults      :tmux:term-keys:emacs:
:PROPERTIES:
:DATE: 2026-02-17
:END:

Terminal Emacs key handling regressed because the container forced
=TERM=tmux-direct= in both generated =devcontainer.json= and shell init.
That bypassed normal terminal negotiation and broke expected key behavior.

** Root cause
- =_jolo/container.py= injected =containerEnv.TERM=tmux-direct=
- =Containerfile= wrote =export TERM=tmux-direct= into =.zshrc.container=
- tmux =terminal-features= config was initially set with =set -g=, which can
  clobber defaults instead of appending

** Fix
- Removed forced TERM from generated devcontainer config and shell init
- Switched tmux feature config to append mode:
  #+begin_src tmux
  set -as terminal-features ",tmux-direct:sixel:extkeys"
  set -as terminal-features ",xterm-256color:sixel:extkeys"
  set -as terminal-features ",wezterm:sixel:extkeys"
  set -as terminal-features ",kitty:extkeys,alacritty:extkeys,foot:extkeys"
  #+end_src
- Added regression test to assert TERM is not forced in =containerEnv=

** Key files
- =_jolo/container.py=
- =Containerfile=
- =tests/test_cli.py=

* Mosh image protocol limits (Sixel/Kitty)                  :mosh:images:term:
:PROPERTIES:
:DATE: 2026-02-18
:END:

Over mosh, image protocols fail even when the local terminal supports them
(Ghostty/kitty/WezTerm/Konsole).

** Findings

- Mosh maintainers explicitly state mosh does not support Sixel or kitty graphics.
- =kitten icat= over mosh fails with "terminal does not support the graphics protocol"
  because mosh does not forward those terminal protocol extensions.
- Over plain SSH, the same host and terminal can render kitty images correctly.

** TERM behavior

- SSH keeps the local terminal type (example: =xterm-ghostty=).
- mosh-server sets remote TERM to =xterm-256color= (or =xterm= in lower-color mode).
  This is expected behavior in mosh internals.

** Practical rule

- Use SSH when you need terminal images (kitty protocol / sixel / iTerm2 inline).
- Use mosh for resilient text workflows (tmux, emacs, shell), not image rendering.

** Refs

- [[https://github.com/mobile-shell/mosh/issues/1081][Mosh #1081]] — SIXEL graphics support (closed)
- [[https://github.com/mobile-shell/mosh/issues/1248][Mosh #1248]] — Kitty graphics protocol support (closed)
- [[https://github.com/mobile-shell/mosh/issues/1274][Mosh #1274]] — maintainer confirms no sixel/kitty support
- [[https://github.com/mobile-shell/mosh/issues/1093][Mosh #1093]] — mosh does not pass full terminal extension stream
- [[https://github.com/mobile-shell/mosh/blob/master/src/frontend/mosh-server.cc][mosh-server.cc]] — sets TERM to =xterm-256color=

* OSC 52 clipboard from Python — write to /dev/tty          :clipboard:osc52:jolo:
:PROPERTIES:
:DATE: 2026-02-18
:END:

Writing OSC 52 to =sys.stdout= fails when stdout is captured by subprocess
pipelines (e.g., during =jolo up= which shells out to =devcontainer= CLI).

** Fix
Write to =/dev/tty= directly, matching the working =just browse= recipe:
#+begin_src python
with open("/dev/tty", "w") as tty:
    tty.write(f"\033]52;c;{encoded}\a")
#+end_src

** Also: busybox base64 wraps at 76 chars
Alpine's =base64= inserts newlines in long output, breaking OSC 52.
Fix: =base64 | tr -d '\n'= in shell, or use Python's =base64.b64encode()=
which doesn't wrap.

* Stale container names after podman system prune           :podman:container:jolo:
:PROPERTIES:
:DATE: 2026-02-18
:END:

=podman system prune -a= can remove images/volumes but leave stopped containers
with claimed names. The =devcontainer= CLI's =--remove-existing-container= flag
only tracks containers it created — if tracking is lost, =docker run= fails with
"name is already in use by an external entity".

** Fix
Before =devcontainer up= with =remove_existing=True=, force-remove by name:
=podman rm -f <container_name>= (silent failure if not found).

* Wayland display passthrough for container Chromium        :wayland:chromium:container:
:PROPERTIES:
:DATE: 2026-02-18
:END:

The container Wayland mount (=WAYLAND_MOUNT= in constants.py) maps the host's
Wayland socket into the container at =/tmp/container-runtime/$WAYLAND_DISPLAY=.
Chromium can render on the host's Wayland compositor with =--ozone-platform=wayland=.

** Key points
- Wayland is per-surface isolated (unlike X11) — safe for containers
- System =chromium= at =/usr/bin/chromium= (Alpine package)
- Works for local dev only — SSH/mosh users get clipboard fallback
- Playwright MCP can also run headed by stripping =--headless= from config

* jolo delete DX: find_git_root escalation and worktree guards  :jolo:delete:security:
:PROPERTIES:
:DATE: 2026-02-21
:END:

Simplified =jolo delete= bare name resolution and found two safety issues
during external reviews (Gemini + Codex).

** Changes
- Bare names resolve as =cwd / name= only (no worktree-by-slug lookup)
- Worktree deletion via interactive picker only
- Interactive purge prompt instead of requiring =--purge= flag

** Issue 1: ancestor git root escalation
=find_git_root(target_path)= traverses up to find =.git=. If someone does
=jolo delete docs= and =docs/= is just a subdirectory, =find_git_root=
returns the parent project root — then =_delete_project= operates on the
wrong repo.

Fix: check =target_path/.git= directly instead of calling =find_git_root=.
The target must itself be a git root.

** Issue 2: worktree path treated as project
=.git= in worktrees is a file (not directory). If someone passes a worktree
path directly (=jolo delete ./proj-worktrees/feat=), the code would call
=_delete_project= on a worktree root.

Fix: check =.git.is_file()= and reject with guidance to use the picker.

** Key files
- =_jolo/commands.py= (=run_delete_mode=, =_delete_project=, =_prompt_purge=, =_purge_dirs=)
- =tests/test_delete.py=

* Podman rootless on btrfs breaks jolo create              :podman:btrfs:rootless:devcontainer:
:PROPERTIES:
:DATE: 2026-02-21
:END:

=jolo create= failed on an Arch laptop with rootless Podman.
Errors:
- =kernel does not support overlay fs ... overlay is not supported over btrfs=
- devcontainers tried to pull =localhost/emacs-gui:latest= from a registry

** Root cause
Rootless Podman defaulted to the =overlay= graph driver on a btrfs-backed home
filesystem, which is unsupported without =fuse-overlayfs=. The existing storage
store became unreadable and emitted missing symlink warnings. devcontainers also
expects a local image named =localhost/emacs-gui:latest=.

** Fix
1. Install =fuse-overlayfs= and force overlay+fuse in
   =~/.config/containers/storage.conf=:
   #+begin_src toml
   [storage]
   driver = "overlay"

   [storage.options]
   mount_program = "/usr/bin/fuse-overlayfs"
   #+end_src
2. Wipe or move the rootless store if corrupt:
   =~/.local/share/containers/storage=
3. Build and tag the image locally:
   #+begin_src sh
   podman build -t emacs-gui .
   podman tag emacs-gui:latest localhost/emacs-gui:latest
   #+end_src

** Notes
- =docker ps= via =podman-docker= still targets rootless storage, not
  =/var/lib/containers/storage= (rootful).
- If you prefer, switching the driver to =btrfs= also works but resets the store.

** Key files
- =~/.config/containers/storage.conf=
- =~/.local/share/containers/storage=
- =Containerfile= (image build)

* agent-shell missing codex-acp in container           :agent-shell:acp:codex:container:
:PROPERTIES:
:DATE: 2026-03-01
:END:

=agent-shell--start= failed with =Executable "codex-acp" not found=.
Trying =npm install -g @zed-industries/claude-code-acp= also failed in this
environment.

** Root cause

The image installed =@openai/codex= but not the ACP adapter package.
=agent-shell= expects the =codex-acp= executable from
=@zed-industries/codex-acp=. The claude ACP package exports
=claude-code-acp=, not =codex-acp=.

** Fix

Added =@zed-industries/codex-acp= to the global pnpm install list in
=Containerfile=.

** Rebuild and verify

#+begin_src bash
jolo up --new
command -v codex-acp
codex-acp --help
#+end_src

** Key file

- =Containerfile=

** Refs

- https://github.com/zed-industries/codex-acp
- https://registry.npmjs.org/@zed-industries/codex-acp

* Template auto-formatting at scaffold time             :templates:formatting:dx:
:PROPERTIES:
:DATE: 2026-03-21
:BRANCH: yolo/format-all-languages
:END:

Added auto-formatting of scaffolded code before initial commit in =jolo create=
to prevent "format on save" whitespace noise in the first real commit.

** Formatters per language

| Language   | Formatter            | Notes                                    |
|------------+----------------------+------------------------------------------|
| TypeScript | =biome check --write= | Already existed; also fixes lint/imports |
| Go         | =gofmt -w .=          | New                                      |
| Python     | =ruff format= + =ruff check --fix= | New; uses =--isolated= to skip template pyproject.toml |
| Rust       | =rustfmt=             | New; requires =apk add rustfmt= (added to Containerfile) |

** Template fixes applied

- Python: line-length wrapping in =web/app.py=, =web/main.py=
- TypeScript: import sorting, quote normalization, tab indentation, =type="button"= a11y fix
- Go/Rust/HTML: =type="button"= on htmx buttons across all web templates
- =layout.tsx=: wrapped =={{PROJECT_NAME}}== in JSX string expression ={"{{PROJECT_NAME}}"}= to prevent biome from adding spaces inside double-braces

** Test fixes

4 pre-existing test failures fixed: assertions expected remote repo URLs for
gitleaks and biome pre-commit hooks, but commit =342d345= switched them to local hooks.

** Key files

- =_jolo/commands.py= — formatting block near line 973
- =templates/lang/*/= — all language template files
- =Containerfile= — added =rustfmt= package

* browser-check bugs: double close, exit code, incomplete help  :browser:tooling:
:PROPERTIES:
:DATE: 2026-03-24
:END:

Critical review of =container/browser-check.js= found several issues.

** Fixed
- Double =browser.close()= — called explicitly then again in =finally=
- =process.exit(1)= on page JS errors — wrong for an inspection tool, many
  real pages throw minor errors. Fatal errors still exit 2.
- =--aria= and =--interactive= flags missing from =--help= output

** Not fixed (noted for later)
- =--output= shared between =--screenshot= and =--pdf= (conflict if both used)
- =--snapshot= always appends =...= even for short content
- Ref system (=e1=, =e2=) useless in stateless tool — dead weight
- No viewport control (=--width=/=--height=)
- =getArg= returns =true= for valueless flags, =parseInt(true)= → =NaN=

** Key files
- =container/browser-check.js=

* jolo create leaves post-init files uncommitted           :jolo:git:scaffold:
:PROPERTIES:
:DATE: 2026-03-24
:END:

=bun init=, =bun add=, =go mod init=, =cargo init= etc. run inside the
container after the initial commit on the host. Generated files (package.json,
lock files, go.mod) were left uncommitted.

** Fix
Added a second commit inside the container after init commands:
=git add . && git diff --cached --quiet || git commit -m "Add dependencies"=

** Key files
- =_jolo/commands.py= — near line 1302

* agent-shell: debugging stalls and permission prompts    :agent-shell:acp:emacs:
:PROPERTIES:
:DATE: 2026-03-24
:END:

** Logging/traffic inspection
- =M-x agent-shell-toggle-logging= — enables ACP protocol logging
- =M-x agent-shell-view-traffic= — raw JSON-RPC messages (requests, responses, permissions)
- =M-x agent-shell-view-acp-logs= — general protocol logs
- =shell-maker-logging= variable — lower-level process logging
- =M-x agent-shell-resume-pending-requests= / =agent-shell-remove-pending-request= — unstick queued requests

** Permission auto-approve
#+begin_src elisp
(setq agent-shell-permission-responder-function #'agent-shell-permission-allow-always)
(setq agent-shell-anthropic-claude-acp-command '("claude-agent-acp" "--dangerously-skip-permissions"))
#+end_src

** Worktree slash commands silently stall
=.claude/= is gitignored so worktrees don't get custom slash commands.
Sending =/afk= (or any custom command) in a worktree causes Claude to go
silent — no error, no response in ACP traffic after =session/prompt=.
Fix: symlink =.claude/= from main repo into worktrees.

* xterm-mouse-mode must be called as function              :emacs:terminal:tmux:
:PROPERTIES:
:DATE: 2026-03-24
:END:

=(setq xterm-mouse-mode t)= does nothing — it's a minor mode, not a variable.
Must use =(xterm-mouse-mode 1)= to actually enable mouse/touch/scroll in terminal Emacs.
Found in =~/.config/emacs/modules/style.el= line 527.

* container/e script doesn't use emacsclient               :emacs:container:daemon:
:PROPERTIES:
:DATE: 2026-03-24
:END:

The =e= launcher was starting a standalone Emacs (=emacs= / =emacs -nw=) instead of
connecting to the daemon. Fixed to use =emacsclient --alternate-editor=""= which
auto-starts the daemon on first call. All subsequent =e= invocations reuse the
same Emacs process, enabling =emacsclient -e= for eval from agents/shells.

** Key files
- =container/e= — the launcher script
- =container/entrypoint.sh= — has =emacs --fg-daemon= behind =START_EMACS=true= (unused by tmuxinator)

* emacsclient -nw on pgtk: works after config fix            :emacs:container:pgtk:
:PROPERTIES:
:DATE: 2026-03-26
:END:

=emacsclient -nw= was crashing instantly on pgtk. Initially misdiagnosed as a
pgtk build bug (switched to gtk3 in commit =4396906=). Actual root cause was
=bergheim/disable-pgtk-im= in Emacs config — see first heading in this file.

** Status
Container runs =emacs-pgtk-nativecomp= and =emacsclient -nw= works. The gtk3
switch was unnecessary and has been reverted. PGTK is the correct choice since
the container uses Wayland.

** Also fixed (same session)
- Added =grep= package (GNU grep) — BusyBox grep lacks =--exclude-dir=

** OSC 52 clipboard from Emacs
Emacs kill ring can sync to host clipboard via OSC 52, same mechanism tmux uses.
Add to Emacs config (guarded by =EMACS_CONTAINER=):

#+begin_src elisp
(when (getenv "EMACS_CONTAINER")
  (setq interprogram-cut-function
        (lambda (text)
          (let ((inhibit-message t))
            (send-string-to-terminal
             (format "\e]52;c;%s\a"
                     (base64-encode-string (encode-coding-string text 'utf-8) t)))))))
#+end_src

Works with PGTK (=send-string-to-terminal= works despite being GUI).
Requires tmux =allow-passthrough all= and =set-clipboard on= (already configured).

** Alpine packages
Alpine provides three nativecomp builds — they conflict, only one at a time:
- =emacs-pgtk-nativecomp= — native Wayland + terminal (use this)
- =emacs-gtk3-nativecomp= — GTK3/X11
- =emacs-x11-nativecomp= — X11 only

* Shift+Space sends literal "u" in terminal Emacs (CSI u decoding) :emacs:tmux:terminal:keys:
:PROPERTIES:
:DATE: 2026-03-26
:END:

Shift+Space in =emacsclient -nw= inserts literal =u= instead of being decoded.
In Evil normal mode: =helpful-key: No command is bound to M-[ 3 2 ; 2=.

** Root cause
Tmux 3.6 defaults to =extended-keys-format csi-u=. When Emacs sends
=\e[>4;1m= (modifyOtherKeys mode 1 request), tmux enables extended keys
but encodes them as CSI u: =ESC [ 32 ; 2 u= (Shift+Space).

Emacs 30's =xterm.el= only decodes the =xterm= modifyOtherKeys format
(=ESC [ 27 ; modifier ; keycode ~=). =xterm-function-map= has zero CSI u
entries. The trailing =u= leaks as a literal character.

Switching =extended-keys-format= to =xterm= also fails — Emacs doesn't
fully decode that format for all keys either (user reported "2%" garbage).

** Diagnosis steps
#+begin_src elisp
;; Check if modify-other-keys is active (nil = not working)
(terminal-parameter nil 'xterm--modify-other-keys)
;; Check TERM on terminal frames
(mapcar (lambda (f) (frame-parameter f 'tty-type)) (frame-list))
;; Daemon initial TERM is "dumb" — expected, not the issue
#+end_src

** Fix
Install =kkp.el= (kitty keyboard protocol for Emacs). Adds full CSI u
decoding to =input-decode-map=. However, =global-kkp-mode= alone is NOT
enough — kkp probes the terminal with =CSI ? u= and tmux doesn't respond,
so kkp concludes "not supported" and never registers its decoders.

Must force-enable kkp's keymaps on tty terminals, bypassing the probe.
In =~/.config/emacs/modules/base.el=:

#+begin_src elisp
(use-package kkp
  :config
  (global-kkp-mode +1)
  ;; tmux doesn't answer the KKP probe but sends CSI u anyway
  (defun kkp-force-enable-on-tty ()
    (let ((terminal (kkp--selected-terminal)))
      (when (and (terminal-live-p terminal)
                 (not (display-graphic-p terminal))
                 (not (member terminal kkp--active-terminal-list)))
        (push terminal kkp--active-terminal-list)
        (kkp-setup-function-keys terminal)
        (set-terminal-parameter terminal 'kkp--previous-normal-erase-is-backspace-val
                                (terminal-parameter terminal 'normal-erase-is-backspace))
        (normal-erase-is-backspace-mode 1)
        (dolist (prefix kkp--key-prefixes)
          (define-key input-decode-map (kkp--csi-escape (string prefix))
                      (lambda (_prompt) (kkp--process-keys prefix)))))))
  (add-hook 'tty-setup-hook #'kkp-force-enable-on-tty))
#+end_src

** Why the probe fails
- kkp sends =CSI ? u CSI [ c= to query KKP support
- tmux doesn't understand =CSI ? u= (not a full KKP terminal)
- Response fails kkp's regex match → "KKP not supported"
- But tmux IS sending CSI u sequences (=extended-keys-format csi-u=)
- =kkp-status= says "enabled" (mode is on) but decoders never registered
- Confirmed via: =kkp--active-terminal-list= is nil, =kkp--setup-visited-terminal-list= has the terminal
- kkp upstream confirms tmux support is out of scope: [[https://github.com/benotn/kkp/issues/12]]

** Persistence bug: force-enable doesn't survive daemon restart
:PROPERTIES:
:DATE: 2026-03-27
:END:

The =tty-setup-hook= approach only works for terminals created AFTER kkp loads.
In daemon mode, elpaca loads packages asynchronously. The first =emacsclient -nw=
may connect (firing =tty-setup-hook=) BEFORE kkp.el is loaded, so
=kkp-force-enable-on-tty= doesn't exist yet. Then =global-kkp-mode= enables,
calls =kkp-enable-in-terminal= for the existing terminal, the async probe fails,
and the terminal is never added to =kkp--active-terminal-list=.

Evidence: =kkp--active-terminal-list= is nil, =kkp--setup-visited-terminal-list=
has the terminal. =tty-setup-hook= has the function. Manual =(kkp-force-enable-on-tty)=
call works immediately.

Attempted fix: =advice-add 'kkp--terminal-setup :after #'kkp-force-enable-on-tty=
to catch probe failure. The callback DOES fire (tmux responds to DA1 =\e[c= but
not =\e[?u=, so kkp's regex fails). Advice should run our force-enable right after.
Still not working — needs debug tracing to confirm the advice fires and what
terminal context it sees.

Also confirmed: =term-keys= (CyberShadow) is loaded but irrelevant — it uses
=\e[27;mod;code~= format, not CSI u. tmux sends CSI u via =extkeys=. The two
packages don't conflict but =term-keys= does nothing in tmux.

** Key files
- =/usr/share/emacs/30.2/lisp/term/tmux.el.gz= — sets =xterm-tmux-extra-capabilities '(modifyOtherKeys)=
- =/usr/share/emacs/30.2/lisp/term/xterm.el.gz= — =xterm--init-modify-other-keys= sends =\e[>4;1m=
- =~/.config/emacs/modules/base.el= — where kkp.el goes
- =~/.cache/emacs/elpaca/sources/kkp/kkp.el= — kkp source, lines 709-733 for the probe logic
